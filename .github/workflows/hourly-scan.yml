name: Hourly Market Scan

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  ping-api:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Market Scan
        run: curl -fsS https://investmentiq.me/api/cron/scan-market